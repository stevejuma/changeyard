# Runtime Migration Notes

Date: 2026-06-09

## Purpose

This file is now a short migration note for the active ChangeYard kanban package. It should not be treated as a plan to reactivate the vendored upstream snapshot.

## Current active surface

- Active runtime entrypoint: `packages/kanban/src/server/index.js`
- Active runtime stack: `packages/kanban/src/runtime-stack/**`
- Active web UI: `packages/kanban/web-ui/src/**`

## Historical cutover summary

- The current package already completed the earlier cutover away from the legacy REST board endpoints and onto the runtime-stack transport surface.
- `/api/health` remains intentionally available as a compatibility endpoint.
- Runtime update endpoints remain wired, but currently report that self-update is unsupported for the private `@changeyard/kanban` package.

## Remaining migration concern

- The remaining work is package ownership and runtime convergence: remove dependence on the vendored reference tree, finish the visible ChangeYard rebrand, and move duplicated Git/JJ workspace logic toward the shared ChangeYard WorkspaceEngine model.
