{
  "name": "@changeyard/docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "pnpm run sync && astro dev",
    "build": "pnpm run sync && astro build",
    "check": "pnpm run build && node scripts/check-links.mjs",
    "preview": "pnpm run sync && astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.4.8",
    "sharp": "^0.35.1"
  }
}
