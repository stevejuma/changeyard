---
name: tools
---

# Tools

Agent tool artifact selection uses `--tools`. Available values are `cursor`, `claude`, `cline`, `codex`, `copilot`, `opencode`, `gemini`, `kiro`, `droid`, `all`, and `none`.

