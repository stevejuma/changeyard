---
name: color
---

# Color

Changeyard uses `--color <always|never|auto>` for human terminal output. `auto` enables color on supported TTYs and respects `NO_COLOR`, `FORCE_COLOR`, and `TERM=dumb`.

JSON and machine-oriented output are never colorized.

