# Changelog

## Unreleased

- Marked `PENDING.md` completion criteria complete across provider-native inline reviews, schema hardening, doctor/recovery flows, VCS depth coverage, release automation, and CLI UX.
- Added scope-diagnostic checks and explicit minimum token guidance in live forge smoke automation.
- Expanded runtime schema tests to cover provider branch rules and error message paths.
- Added deterministic VCS integration tests for workspace push, collisions, and conflict detection paths.
